apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: elyssa
  labels:
    app: frontend
spec:
  selector:
    app: frontend
  ports:
    - protocol: TCP
      port: 7860
      targetPort: 7860
  type: ClusterIP
